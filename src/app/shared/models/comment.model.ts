/**
 *the Comment class
 */
export class  Comment {
    /**
     * comment nimber
     */
    id: number;
    /**
     * the postid that the comment belongs to
     */
    postId: number;
    /**
     * comment name
     */
    name: string;
    /**
     * rmail related to the comment
     */
    email: string;
    /**
     * the content if the comment
     */
    body: string;
}
